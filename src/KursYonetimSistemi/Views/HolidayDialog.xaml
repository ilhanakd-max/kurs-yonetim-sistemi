<Window x:Class="KursYonetimSistemi.Views.HolidayDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tatil" Height="280" Width="420" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Ad" />
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtAd" />

        <Label Grid.Row="1" Grid.Column="0" Content="Başlangıç" />
        <DatePicker Grid.Row="1" Grid.Column="1" x:Name="DpBaslangic" />

        <Label Grid.Row="2" Grid.Column="0" Content="Bitiş" />
        <DatePicker Grid.Row="2" Grid.Column="1" x:Name="DpBitis" />

        <CheckBox Grid.Row="3" Grid.Column="1" x:Name="ChkTekrar" Content="Her Yıl Tekrarlı" />

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Kaydet" Click="Kaydet_Click" Width="120" />
            <Button Content="İptal" Click="Iptal_Click" Width="120" Margin="10,0,0,0" />
        </StackPanel>
    </Grid>
</Window>
